# DB connection config.
# TODO: プロジェクトに応じて適切な値を設定してください

# デフォルトの接続設定プレフィックスは "default." 固定

# デフォルト（SQLite）データベース接続URL（実行時に絶対パスに変換される）
default.conn.url=<ConvertAbsolutePath>jdbc:sqlite:$ApplicationDirPath/example_db/data/example.dbf
# デフォルト（SQLite）接続プールの最大接続数
default.conn.max=3

# Oracle データベース接続URL（TNS記述子形式）
oracle.conn.url=jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))(CONNECT_DATA=(SERVER=DEDICATED)(SERVICE_NAME=ORCL)))
# Oracle データベース認証用ユーザー名
oracle.conn.user=scott
# Oracle データベース認証用パスワード
oracle.conn.pass=tiger
# Oracle 接続プールの最大接続数
oracle.conn.max=10

# PostgreSQL データベース接続URL（ホスト名とデータベース名を設定すること）
posgre.conn.url=jdbc:postgresql://localhost:5432/db01
# PostgreSQL データベース認証用ユーザー名
posgre.conn.user=dbuser01
# PostgreSQL データベース認証用パスワード
posgre.conn.pass=dbpass01
# PostgreSQL 接続プールの最大接続数
posgre.conn.max=10

# SQL実行時間の警告出力閾値（ミリ秒）。この時間を超えるとログに警告を出力
sqlexec.warn.time=3000
